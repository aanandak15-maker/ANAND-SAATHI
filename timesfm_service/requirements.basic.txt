# Basic dependencies without dependency conflicts
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
python-multipart>=0.0.6
python-dotenv>=1.0.0

# For simulation mode (works without TimesFM)
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.11.0
scikit-learn>=1.3.0

# Note: TimesFM installation will be attempted separately to avoid conflicts
